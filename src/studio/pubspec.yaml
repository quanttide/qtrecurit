name: qtrecurit_studio
description: "量潮招聘"
publish_to: "none"
version: 0.1.0-alpha.1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    # mock 数据：本地演示，后续替换为 API
    - assets/mock/recommendations.json
